{
  "scripts": {
    "infra:up": "sudo docker compose -f docker-compose.infra.yml up -d",
    "up": "sudo docker compose -f docker-compose.infra.yml -f docker-compose.apps.yml up -d",
    "pull": "sudo docker compose -f docker-compose.infra.yml -f docker-compose.apps.yml pull"
  }
}